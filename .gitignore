# Never commit model artifacts.
*.gguf

# Go build outputs.
tools/validate/validate
*.test

# Editor/OS noise.
.DS_Store
*.swp
.idea/
.vscode/

# Flat catalogs emitted by -emit-flat (CI uploads them; never commit).
/catalog.json
/catalog-staging.json
